# Internal documentation hub — deploy behind VPN / SSO in production.
spring.application.name=engineering-docs-hub
server.port=8095

# Thymeleaf cache off for local iteration
spring.thymeleaf.cache=false
